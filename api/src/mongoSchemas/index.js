const Error = require('./Error');
const Game = require('./Game');
const Message = require('./Message');
const Profile = require('./Review');
const User = require('./User');

module.exports = { Error, Game, Message, Profile, Review, User };
